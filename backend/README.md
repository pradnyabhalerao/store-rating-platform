# Store Rating Platform Backend

Backend scaffolding for the Store Rating Platform.

## Available scripts

- `npm install`
- `npm run dev`
- `npm start`

## Environment

Create a `.env` file with:

```text
PORT=5000
MONGO_URI=mongodb://localhost:27017/store_rating_platform
JWT_SECRET=your_jwt_secret
```
